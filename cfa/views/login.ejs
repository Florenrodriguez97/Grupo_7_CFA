<!DOCTYPE html>
<html lang="en">
<%- include('partials/head.ejs') %>

  <body>

    <%- include('partials/header.ejs') %>

      <div class="container">

        <div class="container form">
          <div class="col-md-12 text-center">
            <form class="container login" action="" method="POST">
              <h1>Iniciar Sesion</h1>
              

              <input name="usuario" type="text" id="usuario" value="<%= (typeof data!='undefined' && data.usuario)?data.usuario : "" %>" placeholder="Ingresa Usuario" >
              <p><%= (typeof errores!='undefined' && errores.usuario)?errores.usuario.msg : "" %></p>

              <input name="contrasenia" type="password" id="contrasenia" value="<%= (typeof data!='undefined' && data.contrasenia)?data.contrasenia : "" %>"placeholder="Ingresa Password" >
              <p><%= (typeof errores!='undefined' && errores.contrasenia)?errores.contrasenia.msg : "" %></p>

              <button type="submit" class="btn btn-primary btn-login">Iniciar sesion</button>
              <div>
                <a href="/users/registro">
                  ¿No estas registrado?
                </a>
              </div>

            </form>
          </div>
        </div>
      </div>
      <%- include('partials/footer.ejs') %>

  </body>

</html>